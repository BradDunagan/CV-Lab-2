node_modules/
build/
dist/
out/
*.node
.DS_Store
assets/
sessions/

# Personal reference notes live alongside the docs as .txt; the project's own
# documentation is .md. Keeps `git add -A` from sweeping them in.
docs/*.txt
