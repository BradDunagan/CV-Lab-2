# 2026-08-25

First entry. Setting up the notes convention — see `notes/README.md`.

## Where the project is

The lab goes from a PNG to described geometry:

```
A  = load("cube.png", as=linear)
G  = gray(A)
B  = gaussian(G, sigma=1.4)
Gx = sobel(B, axis=x)
Gy = sobel(B, axis=y)
M  = sobel(B, axis=mag)
N  = nms(M, Gx, Gy)
S  = segments(N, Gx, Gy)
R  = merge(S)
F  = fit(R)
C  = corners(F)
```

Sixteen operations. 270 tests across nine suites, green on macOS, Linux and
Windows.

## Numbers from the cube, for reference

`assets/cube-1-256x256.png`, 256×256:

- 9 segments after `merge`, 42–63 px, worst residual 0.00–0.83
- angles cluster in three families — 0–16°, 77–85°, 139–157° — which is three
  visible faces
- 14 corner candidates, of which **4 have three agreeing segments** (a cube in
  general position has exactly four vertices where three visible edges meet)
  and 2 more have a reach of 1.1 and 1.4 px (the silhouette vertices)
- the other 8 reach 46–92 px and are spurious

`minMag=0.005`, not the original 0.02. The nms magnitude on this image peaks at
**0.0585** — an order of magnitude below a synthetic 0→1 step.

## Open questions

- Should `load` default to `as=linear` rather than `as=srgb`? Still open in
  `design-lab-model.md` §11.
- Does `pt-lab-workspace` write gamma-encoded or linear PNGs? Every image in
  `assets/` declares nothing, so the lab assumes sRGB by convention. If the
  renderer writes linear, that assumption is wrong and every `toLinear` applies
  a curve that was never there.
- Higher-precision input needs a native decoder — Chromium gives 8-bit RGBA.
  Worth doing only when an experiment actually needs the precision.

## Next

The follow-up analysis that `corners` was built to make possible: go back to
the image and test a hypothesis, but only where the geometry says it might pay
off. Check whether gradient magnitude is elevated along an extrapolated path,
or re-run `segments` locally with a lower threshold near a predicted corner.
Separate operation, consuming corners.

## The session

    /status -

    Version:          2.1.221
    Session name:     Electron cross-platform framework overview
    Session ID:       04fd7b28-8c66-4e94-8fb5-27b7cce6f1bc
    Session kind:     interactive
    cwd:              /Users/bradsmac/dev/ai/cv-lab-2
    Login method:     Claude Max account
    Organization:     brad.dunagan@gmail.com's Organization
    Email:            brad.dunagan@gmail.com

    Model:            Default (Opus 5 with 1M context · Best for everyday, complex tasks)
    MCP servers:      1 connected, 3 need auth, 1 disabled · /mcp
    Setting sources:  User settings, Project local settings

## For the next session

❯ At this point I have the following (seeming large task) things in mind, in order of doing: 1) A careful review of the project, making sure the code corresponds to the docs (and the other way around), and that the docs are coherent. 2) Use ../paneless-workspace/packages/paneless for the UI, like ../rr does. 3) From this app call ../pt-lab-workspace/packages/pt-lab to generate images with varying object positions, lighting, etc.. Is there anything else that should be done before considering these?

...

Your ordering stands as you had it:

1. Review — core only (native/, src/lab/, docs). Treat src/renderer/ as provisional since task 2 replaces it.
2. paneless UI
3. pt-lab batch generation — with a headless CLI runner folded in.

The one number to carry into task 3: cost is quadratic in segment count, which is a property of the scene, not the resolution. A megapixel of pixel work is ~30 ms. Watch segment counts as you vary lighting and position — a few hundred is milliseconds, a few thousand and corners becomes the next wall at around 2.7 s.

Everything is pushed and the tree is clean.


❯ After a long session I tend to be wary of starting a new session because I am afraid that some information will not be available. Now, in this session, the context is full/used-up. Am I being paranoid? In any case, provide for me a prompt to help get started in the next session.

Let me check what actually survives before answering.

  Ran 1 shell command

Not paranoid in general — but let me give you the actual numbers, because they say something specific about this session.

What survives

41 commits, averaging 40 lines of message each. That's unusually dense, and deliberately so: every non-obvious decision this session went into a commit message or a doc, including the ones where I was wrong and corrected myself. The git log is a real record, not a changelog.

Plus three documents that carry the reasoning — electron-guide.md (written after doing), design-lab-model.md (written before, corrected after), glossary.md — and now notes/.

So the knowledge is durable. What's lost is only the conversational thread, and most of that was persisted on purpose.

The one real gap

There's no CLAUDE.md. That's the file a new session reads automatically. Without it, a new session starts knowing nothing about this project and has to be pointed at everything by hand — which is exactly the friction you're describing.

That's the durable fix, not a good opening prompt. A prompt helps once; CLAUDE.md helps every time, including sessions where you forget to paste anything. I'd also add a cv-lab-2 entry to the shared dev/ai memory, which currently has five world-lab entries and nothing about this project.

Want me to write both? It's ten minutes and it retires the worry permanently.

Meanwhile, here's the prompt

Working on cv-lab-2 (~/dev/ai/cv-lab-2) — an Electron + native C
computer-vision lab. Read these first, in order:

  docs/design-lab-model.md   the model: slots, ops, the log, reproducibility
  docs/electron-guide.md     builds, CI, packaging, the Electron constraints
  docs/glossary.md           terms used in both
  notes/brads-notes/         my working notes

Then `git log --oneline -40` — the commit messages carry the reasoning,
including several places where a decision was made and later corrected.

State: 16 operations, load → gray → gaussian → sobel → nms → hysteresis /
orient → segments → merge → fit → corners. 274 tests across nine suites,
green on macOS, Linux and Windows. `npm test` runs everything.

The plan, in order:

  1. Review the lab core (native/, src/lab/, docs) for code/doc agreement
     and coherence. Skip src/renderer/ — step 2 replaces it.
  2. Port the UI to ../paneless-workspace/packages/paneless, the way ../rr does.
  3. Call ../pt-lab-workspace/packages/pt-lab to generate images with varying
     object position and lighting, and run the pipeline over them in batch.
     Needs a headless CLI runner; Session.replay exists, an entry point doesn't.

Two things measured recently that matter for planning:

  - Cost is quadratic in SEGMENT COUNT, not resolution. A megapixel of pixel
    work is ~30 ms; a busy scene is what hurts. `corners` is the next wall at
    ~2.7 s once counts get into the tens of thousands.
  - Open questions live in design-lab-model.md §11: whether `load` should
    default to as=linear, and whether pt-lab writes gamma-encoded or linear
    PNGs (every image in assets/ declares nothing, so the lab assumes sRGB).

Start with step 1.

One thing to check when you get to step 3, since it's cheap and would invalidate work if wrong: whether pt-lab writes gamma-encoded or linear PNGs. It's in the prompt and in the notes because it's the kind of assumption that quietly corrupts everything downstream.
